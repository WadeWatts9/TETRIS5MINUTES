version: '3.8'

services:
  tetris:
    image: ghcr.io/wadewatts9/tetris5minutes:latest
    container_name: tetris-teresiano
    restart: unless-stopped
    ports:
      - "3002:3002"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3002
      - NODE_ENV=production
